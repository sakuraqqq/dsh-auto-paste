# dsh-auto-paste — 验证/交接记录（2026-09-04）

> 本工作区（负责 dsh-auto-paste 后续维护）接手前必读。上一轮在 `dsh-workspace\插件` 会话里完成了：构建修复 + 静态 API 检查 + 独立 profile 实测 + GUI 实测。

## 一句话交接

**dsh-auto-paste v0.1.2 已确认在 dsh 0.1.1-rc.2 上无 bug（host + client 全流程实测通过）。** 后续若要升级到更新版 dsh 或加功能，按本文档的「版本线」和「验证流程」走。

## 插件状态

| | |
|---|---|
| 包名/版本 | `dsh-auto-paste` **v0.1.2** |
| 仓库 | https://github.com/sakuraqqq/dsh-auto-paste |
| 快照位置 | `<快照目录>`（**发布快照，无 .git**） |
| 形态 | host + client + typert 三半身（`dsh.bundle.patch` + `dsh.client.inject:["sessions","connection"]`） |
| 目标 dsh | **0.1.1-rc.2**（当前运行版本；另有 alpha 0.1.2-alpha.3 在推） |

## 本轮做了什么（供追溯）

### 1. 发现并修复构建依赖解析 bug（`npm install` ERESOLVE）
- **现象**：`npm install` 报 `ERESOLVE`；随后 `typecheck`/`build` 报「`tsc` 不是内部或外部命令」（连锁，非真没 tsc）。
- **根因**（命中 dsh-plugin-dev 技能坑 #1）：插件 `devDependencies` 锁 `@deepseek-ai/dsh-session@0.1.0-rc.6`，但 peer `dsh-tools >=0.1.0-rc.6` 让 npm 去拉 **`@deepseek-ai/dsh-tools` 的 npm `latest` = 0.1.0-rc.8（过期老版本）**；rc.8 的 dsh-tools 带 peer `dsh-agent@rc.8` → `dsh-session@^rc.8`，与锁的 `dsh-session@rc.6` 撞车。
- **修法**：把 `@deepseek-ai/dsh-tools`、`@deepseek-ai/dsh-typert-protocol`、`@deepseek-ai/dsh-session` 在 peer+dev 里**锁到 0.1.1-rc.2**（对齐实际运行的 dsh）。修正版在 `dsh-auto-paste-src/package.fixed.json`。
- **关键逻辑**：devDeps 里**精确锁定** `0.1.1-rc.2`，npm 按此取，不被过期的 `latest=rc.8` 带偏；rc.8 不满足 `^0.1.1-rc.2`，整棵树走到 0.1.1 线。

### 2. 静态 API 兼容检查（对照已装 0.1.1-rc.2）
- ✅ 在：`ctx.get('workspaceRegistry')`（`class WorkspaceRegistry extends Service`）、`TypertRemoteService`（`extends Service`）、`defineTool`（`function defineTool`）、session `header.cwd`（绝对路径校验）。
- ⚠️ 曾标待确认（后实测无恙）：`type { ToolExecution }`、client 的 `sessions.list.getSnapshot().current`、`exec.agent?.session?.header?.cwd`。
- **结论**：类型层面 `tsc` 编译通过；client 为纯 JS 不经 tsc，靠运行时验证——本次运行时 OK。

### 3. 构建
- `npm run build`：`tsc -p tsconfig.json` 通过 + `scripts/sync-lib.mjs` 成功（`dist/index.js`、`dist/typert.host.js` → `lib/`）。
- ⚠️ client bundle 走 `build:client`（`scripts/sync-client.mjs`），独立构建；改 client 要跑 `npm run build:all`。

### 4. 独立 profile 实测
```powershell
dsh plugin --profile autopaste-test add file:<快照目录>
dsh --profile autopaste-test
# 输出: [dsh-auto-paste] host ready — pasteStore service + save_paste tool listed=true (minChars=500)
```

### 5. GUI 实测
```
[dsh-auto-paste] client paste listener ready (minChars=500)
[dsh-auto-paste] saved paste (1994 chars) -> pastes/20260904-002311834.txt
[dsh-auto-paste] saved paste (7224 chars) -> pastes/20260904-003018714.txt
```
- 无 `savePaste failed` / `services unavailable` / `falling back` / `Uncaught/TypeError`。
- **路径引用在输入框正常出现** → 连 skill 坑 #6（React 回退）也 OK。

## 验证结论

**核心流程在 dsh 0.1.1-rc.2 上无 bug**（构建、host 加载、tool 注册、client 粘贴落盘 + 路径引用全部正常）。之前担心的 3 处 API 漂移点实测全部无恙。

## 遗留 / 给后续工作区的注意

- **版本线**：所有 `@deepseek-ai/*` 必须锁**同一条线**（如全 0.1.1-rc.2），`peer`+`dev` 里用；**不要在 `dependencies` 里声明 `@deepseek-ai/*`**（否则 pnpm 装出两份 closure，出诡异 `Cannot read properties of undefined`）。
- **构建要用「锁 0.1.1-rc.2」的 package.json**（`package.fixed.json`）；否则独立 `npm install` 会被过期的 `latest=rc.8` 带偏 → ERESOLVE。
- **dsh 再升级要回归**（dsh-plugin-dev 技能点名）：
  - 坑 #7：client 捕获阶段 `event.stopImmediatePropagation()` 会屏蔽 dsh 核心 paste 逻辑 —— dsh 升级重点回归。
  - 坑 #6：`document.execCommand('insertText')`（deprecated）+ `setRangeText` 兜底不触发 input 事件 —— React 大版本升级需重测。
  - client 是纯 JS，`sessions.list.getSnapshot().current` / RPC 签名每次 dsh 升级都要运行时验证。
- **升级 dsh 前先现场核实**：`npm view @deepseek-ai/dsh-tools dist-tags`（别盲信本文档的版本结论，dsh 处于 rc 高速迭代）。

## 功能 backlog（下一步可做，按对接单的实用度排序）

- **A. 实现 `label` 参数**（现声明但 unused）→ `pastes/<ts>-<label>.txt`
- **B. `maxBytes` 做成可配置**（现硬编码 1 MiB）→ 进 `cordis.patch.yml`
- **C. `minChars` 加设置页 UI 可调**（现 host/client 各读 config，易漂移 → 统一 host 下发）
- **D. 保存 + 摘要**（路径引用 + 一句话摘要，省后续展开）
- **E. 保存后轻提示 / 可撤销**
- **F. session↔workspace 严格映射**（现匹配不到 session 会回退到第一个 workspace，多工作区场景可能存错）

## 相关文件（在本工作区 `dsh-auto-paste-src\` / 根）

- `dsh-auto-paste-verification.md` —— 完整分析 + 边界用例 runbook + 功能清单
- `dsh-auto-paste-src\package.fixed.json` —— 修正版 package.json（锁 0.1.1-rc.2）
- `dsh-auto-paste-src\index.ts / client.js / typert.host.ts` —— 本轮读过的源码副本
- `dsh-auto-paste-src\console.log` —— GUI 实测控制台导出（100KB）

## 红线（延续）

- 测试插件一律**独立 profile**，不装主 web。
- 插件**默认零外发数据**（只是本地落盘）——维持，确需网络能力要在 README/审查清单显式声明。
- 发布动作（git push / npm publish）在你自己终端执行。

---

## 附：本工作区（dsh-auto-paste）落地记录（2026-09-04，执行方）

> 此前修法仅存在于 `dsh-workspace\插件`（snapshot 路径），**未同步进本仓库**。本次把已验证的修法落到本工作区并回归，与 `package.fixed.json` 对齐。原 `HANDOFF-版本兼容性测试.md` 里的「方案 B：暂不升级」已在用户新的拍板下**被覆盖**（现按方案 B 落地）。

### 改动（仅 3 个文件，`src/` 零改动）

| 文件 | 结果 |
|---|---|
| `package.json` | `@deepseek-ai/dsh-tools`、`dsh-typert-protocol` 从 `dependencies` 移到 `peer`+`dev`，全部锁 **0.1.1-rc.2**（exact）；`dsh-session` dev → **0.1.1-rc.2**；`dependencies` 仅剩 `zod`。与 `package.fixed.json` 结构完全一致。 |
| `pnpm-lock.yaml` | 由 `pnpm install` 重新解析（无 0.1.0-rc.x 残留）。 |
| `tests/release.test.js` | 静态守卫断言从 `pkg.dependencies[...]` 改为 `pkg.peerDependencies[...]`（guard 意图「必须声明该包」不变，位置对新结构修正）。**改断言 = 改口径，已由用户拍板确认。** |

### 回归结果（本工作区，pnpm）

- `pnpm install`：`dsh-tools`/`dsh-typert-protocol`/`dsh-session` 全部 0.1.1-rc.2，cordis 4.0.1，zod 在；无 0.1.0-rc.6/rc.8 混线。
- `typecheck` exit 0；`build` exit 0；`test` **18/18 全绿**（含同秒并发/越界/单测回归/静态守卫）。
- 独立 profile `autopaste-test` 重装：插件**不再自带一份 dsh-tools**（peer 模型，无自嵌套 node_modules，`pnpm why` 空），由运行时提供。
- host-load 探针：`[dsh-auto-paste] host ready — pasteStore service + save_paste tool listed=true (minChars=500)` → **peer 解析运行正常**。

### 产物（SHA256）

- `package.json`：1966B，`CE21DE01A5D6DBEBEF65EF8A0695437CC46A2B9AAFC5FEFAB026FE1ABE6FD3E2`
- `pnpm-lock.yaml`：21083B，`F9E84B6BDD411F6683EC518046FFB8D6404FD25C28742E831CEE46104629A2C0`
- `tests/release.test.js`：8226B，`F10E79603082563FEF3A1F8E6BCA6F540E8FB4BC1D0CF3F3E9F00DD0D82F24EB`

> 结论：版本线已对齐 0.1.1-rc.2，双闭包消除；发布动作留给用户在终端执行（AI 只到 dry-run）。
