# 转接/记录：dsh-auto-paste 适配 dsh 0.1.5-rc.1（2026-09-10）

> 上一轮（0.1.1-rc.2）见 `HANDOFF-rc2验证-20260904.md`。本轮是 **dsh 大更新（0.1.1-rc.2 → 0.1.5-rc.1）导致插件失效** 的根因定位、修复，以及首套「防屎山」质量门落地。

## 一句话交接

**根因：dsh 0.1.5-rc.1 把 composer 从 `<textarea>` 换成了 Lexical `contenteditable` div，插件的 composer 判定（要求 `tagName==='TEXTAREA'`）归零，大段粘贴静默失效。** 已修 client 半身 + 依赖升到 0.1.5-rc.1 + 版本 bump 0.1.3 + 落 P1/P2/P3 质量门，自动门禁全绿；**真机粘贴验证待用户执行**。

## 三要素（磁盘实测回读）

| 项 | 值 | 实测方式 |
|---|---|---|
| 项目名 | dsh-auto-paste | 工作区根 |
| 插件版本 | **0.1.3**（本轮由 0.1.2 bump） | `package.json` `version` |
| 插件 dsh 依赖线 | **0.1.5-rc.1**（dsh-tools / dsh-typert-protocol / dev dsh-session，exact，peer+dev） | `package.json` |
| 基线 SHA | `7e317371fbb0bec9916ad09c0124cec5dfa98c13` | `git rev-parse HEAD` |
| 目标 dsh | **0.1.5-rc.1**（上一轮 0.1.1-rc.2） | `dsh --version` |

## 根因（证据链，非猜测）

- **症状**：新 dsh 下往输入框粘贴 ≥500 字符 → **完全没反应（无日志无文件）**（用户确认）。
- **证据①**：`dsh-client-ui-conversation/lib/client.js` 注释明写 —— *"The composer's contenteditable host: binds one shell-owned Lexical editor"* / *"@returns the resident contenteditable div"*。
- **证据②**：该包内全搜 `"textarea"` **零匹配**（仅剩描述旧设计的注释，如 "of the old textarea"）。
- **证据③**：实际 DOM —— `<div data-input-scroll> → <div class="grow"> → <ComposerContentEditable data-phase contenteditable>`。
- **证据④**：插件 `isComposerTextarea()` 首行 `if (target.tagName !== 'TEXTAREA') return false` → 新 composer 恒 false → paste 处理器静默 return。
- **排除**：`sessions.list.getSnapshot().current` 形状未变（新 dsh 自用，`dsh-client-ui-conversation:16562`）；`data-phase` / `data-input-scroll` 锚点仍在（只是元素 tagName 变了）。

## 修复（仅 client 半身 + 依赖/版本）

| 文件 | 改动（旧 → 新） |
|---|---|
| `src/client.js` | `isComposerTextarea` → **`isComposerTarget`**：`TEXTAREA \|\| isContentEditable` + `data-phase` / `closest('[data-input-scroll]')` 锚点；新增 `insertIntoContentEditable`（Selection API + `InputEvent`）兜底；`execCommand('insertText')` 抽为 `insertViaExecCommand` 作主路径。 |
| `package.json` | 三个 `@deepseek-ai/dsh-*` `0.1.1-rc.2 → 0.1.5-rc.1`（exact，peer+dev）；`version 0.1.2 → 0.1.3`；`@deepseek-ai/cordis` 保持 `^4.0.1`（兼容运行时 `^4.0.2`，收紧会排除 4.0.1 消费者）。 |
| `tests/release.test.js` | 新增静态守卫：`src/client.js` 的 composer 判定必须同时含 `TEXTAREA` 与 `isContentEditable`。 |

## 防屎山 P1/P2/P3（本轮新落，对标 doc2md）

| 层 | 产物 | 门禁 |
|---|---|---|
| **P1 静态** | `eslint.config.js`（flat，@eslint/js recommended + `complexity` warn；白名单：`src/client.js` + `scripts/**/*.mjs` + `smoke.mjs`；`.ts` 交给 tsc、`tests/` 自证）+ `.prettierrc`（`semi:false, singleQuote:true, printWidth:100`） | `pnpm run lint` exit 0；`pnpm run format:check` 全过 |
| **P2 复杂度** | `tools/metrics.mjs`（espree AST；圈 ≤10 / 认知 ≤15；写 `CODE-METRICS.md`） | `pnpm run metrics` **超限函数数必须为 0**，否则 exit 1 |
| **P3 CI** | `.github/workflows/ci.yml` | install → typecheck → build+build:client → 产物=源码(`git diff --exit-code -- dist lib`) → lint → format:check → metrics → test → smoke |

**P2 口径说明（重要，防假阳性）**：嵌套函数体**不计入外层函数**（标准圈复杂度口径）。否则 `__ModuleLoader__` 的 `factory` 包装函数会把整包内层分支算进自己（实测假阳性 cyc=31）；修正后该假阳性消失，超限 0。
**lint 顺手修的两个真问题**：`smoke.mjs` 未使用导入 `node:os`；`src/client.js` 无用赋值（eslint 10 `no-useless-assignment`）。

## 回归结果（全绿）

- `pnpm install`：三包升到 0.1.5-rc.1（`dsh-tools` npm `latest` 仍指向过期的 `0.0.1-rc.1`，真实线在 `next` —— 技能坑 #1 再验证）
- `typecheck` exit 0 · `build` exit 0 · `build:client` exit 0 · `lint` exit 0 · `format:check` 全过 · `metrics` **超限 0** · `test` **19/19** · `smoke.mjs` **SMOKE-OK**
- 产物双写确认：`dist/client.js`（tsc 产物，exports/loader 用）+ `lib/client.js`（sync-client，super-injector 用）**均含修复**
- **快照已同步**：`<快照目录>` 的 `dist/client.js` / `lib/client.js` / `src/client.js` / `package.json` 与工作区**逐文件 SHA256 一致**

## 拍板记录（用户）

1. **A+B+C+D 全做**：修复范围=仅 client / 插入方法=execCommand 主路径 + contenteditable 兜底 / 版本线=升 0.1.5-rc.1 / 新增静态守卫断言。
2. **同步到快照**：允许把工作区构建复制进部署快照（供真机验证）。
3. **`pnpm-workspace.yaml` 保留并提交**（pnpm 自动写的 minimumReleaseAgeExclude 15 条，0.1.5-rc.1 过 minimumReleaseAge 策略所必需）。
4. **防屎山做 P1+P2+P3**（不做 P4 jscpd 重复率门）。

## 真机验证（已通过 ✅ 2026-09-10）

- **结果**：用户在新 dsh（0.1.5-rc.1）GUI 中往输入框粘贴 **6158 字符** → **成功落盘** `pastes/20260910-204631429.txt`（11724B），且 composer 正确出现路径引用 `[已保存大段粘贴为附件: pastes/20260910-204631429.txt (6158 字符)]`。用户确认「可以」。
- **证明**：检测路径（contenteditable 识别）与插入路径（`execCommand` → Lexical `beforeinput`）**真机双通** —— 本轮修复端到端闭环。
- 测试产物 `pastes/` 已在 `.gitignore`（第 5 行），不污染仓库。

## 待办

1. **发布**（用户终端执行；AI 只到 dry-run）：dry-run 已核 → `npm pack --dry-run --json` = `dsh-auto-paste-0.1.3.tgz`（14110B / 10 文件：LICENSE、`dist/{index,client,typert.host}.js`、`src/`、`cordis.patch.yml`、`package.json` 齐）。`npm publish --tag next` → 观察 ≥3 天 → 转正 `npm dist-tag add dsh-auto-paste@0.1.3 latest`。
2. **git**：先 commit 版本 bump 再打 tag（顺序反了 tag 会指错 commit）；`git push` 由用户执行，禁 `--tags`。
3. 快照未镜像本轮新增的**开发用**文件（`eslint.config.js` / `.prettierrc` / `tools/` / `CODE-METRICS.md` / `.github/`）——运行时不需要，如需完整镜像再同步。

## 复盘（坑现象 → 根因 → 防再犯）

- **坑现象**：dsh 从 0.1.1-rc.2 升到 0.1.5-rc.1 后，插件 host / client **都"加载成功"**（两条 ready 日志都在），但大段粘贴**完全没反应**、无任何报错。
- **根因（一句话）**：dsh 把 composer 从 `<textarea>` 换成 Lexical `contenteditable` div，而插件判定函数把 `tagName === 'TEXTAREA'` 当成硬前提 → 命中率归零 → 静默 return。
- **防再犯**：
  1. composer 判定改**锚点优先**（`isContentEditable || TEXTAREA` + `data-phase` / `[data-input-scroll]`），不再绑定单一 tagName；
  2. 新增静态守卫断言锁住「必须同时识别两种 composer」（`tests/release.test.js`）；
  3. **静默 return 是本次最难查的一环**——纯靠「加载正常但功能无反应」的二分法 + 源码比对才发现。后续新增 early-return 分支需带可诊断日志，不留无痕出口；
  4. 升级 dsh 前先 `npm view <pkg> dist-tags` 现场核实（`latest` 长期指向过期线，真实线在 `next`），再 exact 锁同一条线（技能坑 #1 二次命中）。
  5. **公开仓库隐私泄漏（红线 #11，本轮发现并处置）**：根因 = 内部交接文档被当作仓库文档提交进了公开仓库，而它们天然含本地环境信息。实测命中：6 个已跟踪文档含本地绝对路径（Windows 用户目录下的工作区/快照路径）、1 个文档含 QQ 邮箱。**处置**：全部占位符化（`<工作区>` / `<快照目录>` / `<rc8实验环境>` / `<旧实验环境>`）+ 邮箱移除 + `.gitignore` 增 `HANDOFF-*.md` / `DEV-NOTES.md` 防今后入库 + 全量敏感串自查（邮箱/QQ/手机/身份证式/本地路径）**归零**。历史旧提交仍留痕（用户拍板接受，本轮不做 `filter-repo`）。**防再犯**：写文档一律用占位符/相对路径，不写绝对路径；`git push` 前跑一次敏感串自查。

## 产物 SHA256（2026-09-10 实测）

| 文件 | 字节 | SHA256 |
|---|---|---|
| `package.json` | 2346 | `FC33F04F8E24725EC2260562811BFA8F6026A2433A2F024C61F732BCFCE98339` |
| `src/client.js` | 7666 | `1399A3E9FAFE015A802AD2FEAC76EDD837CC32D0EB25E7A7CC9D59A58A896CC6` |
| `dist/client.js` | 8676 | `9CF493637EA43E0FDEE6C6ED9C875764D8A6D298B0B93895082DAFA031A5F1FC` |
| `lib/client.js` | 7666 | `1399A3E9FAFE015A802AD2FEAC76EDD837CC32D0EB25E7A7CC9D59A58A896CC6`（= src 逐字节） |
| `tests/release.test.js` | 8873 | `33824B85DFC3F571D6229EB9B68FE17FD55FB4EACD4FDC3CD6A23DD2AC22D73B` |
| `pnpm-lock.yaml` | 38078 | `8113F18F2385EC695DFD37CA1B099FC56185A0E790F45C42EB91E252FA197D9F` |
| `pnpm-workspace.yaml` | 691 | `942BFCD89BF701031D1F9C913D64CE52F80FB33577970499F022005BB483E265` |
| `eslint.config.js` | 1787 | `0751AE6AAB7EDF62D584FD0B856C998C4DEEAEF73B3B3FCEDF40AEA353DC5449` |
| `.prettierrc` | 90 | `D423A7296D893AC8A1B8EB49D11A7C12293C93B3FD534C9B16BA7AF632FFB276` |
| `tools/metrics.mjs` | 9711 | `ACDA4E12FCFD31E92C77982706D7D6122A091B6DF2EF381A3108B91010CE5AB5` |
| `CODE-METRICS.md` | 2455 | `951ADB09E196F3645913D61097A13FD9BC8FBD373893EFC177F75E966265E1D0` |
| `.github/workflows/ci.yml` | 1853 | `D41F474B7C2B1A1913922A7A152E38AF51262C4D91924E3D8A583D9333F6E6DE` |
| `smoke.mjs` | 3856 | `DF476E0CF66DF132DEA877396302DC98EE3BFC374093AE6E871B25813E3E8841` |
| `scripts/release.mjs` | 10489 | `8CC0390AB38B58BCAE7EC80B16E76BDF0A0886A74A5C96B8ACF4C9F5C2208160` |
| `scripts/sync-client.mjs` | 779 | `BD3E083DEFF7A71D040DFAB17249A61BE1186FE2BF37F7870E0CA43369BBB5EB` |
| `scripts/sync-lib.mjs` | 1151 | `B744C58E158250C3D42B8C03BD72780922B7CE0F3D248C5C6E053E4CF117F8D3` |
| `HANDOFF-rc15修复-20260910.md` | 5654 | `1455306C8694E36C83EC44B543889C15059D823FD216FD1A7063BFD1C7A5949D`（本节改写后以磁盘回读为准） |

## 红线（延续）

- 测试插件一律**独立 profile**，不装主 web。
- 发布动作（`git push` / `npm publish`）由用户终端执行；AI 只到 dry-run。
- 改配置文件（package.json / pnpm-workspace.yaml / cordis.patch.yml）前备份、改后回读校验。
- 改 client 半身必须跑 **`build` + `build:client` 两条**（`build` 只同步 host；client 走独立的 `sync-client.mjs`）。
